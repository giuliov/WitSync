<UserControl x:Class="WitSyncGUI.View.GlobalListsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:WitSyncGUI.ViewModel"
             d:DesignHeight="500"
             d:DesignWidth="750"
             mc:Ignorable="d">
    <UserControl.Resources>
        <vm:GlobalListsViewModel x:Key="viewModel" />
    </UserControl.Resources>
    <Grid DataContext="{Binding Source={StaticResource viewModel}}">
        <Label Content="Included Global lists" />
        <ListBox ItemsSource="{Binding Path=Included}" BorderBrush="AliceBlue" BorderThickness="2">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <CheckBox Content="{Binding Path=Item}" IsChecked="{Binding Path=IsSelected}" />
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</UserControl>
